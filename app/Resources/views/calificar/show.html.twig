{% extends 'base.html.twig' %}

{% block body %}
    <h1>Calificar</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ calificar.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ calificar.descripcion }}</td>
            </tr>
            <tr>
                <th>Puntos</th>
                <td>{{ calificar.puntos }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('calificar_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('calificar_edit', { 'id': calificar.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
