{% extends 'base_logueado.html.twig' %}

{% block contenido %}
    <h1>Editar Reputación </h1>

{{ form_start(edit_form) }}
    <div class="row">
        <div class="col-sm-12">
            {{form_row(edit_form.descripcion, {'attr':{'class':'form-control'}} ) }}
        </div>
    </div>
    <br>
    <div class="row">
        <div class="col-sm-6">
            {{form_row(edit_form.maximo, {'attr':{'class':'form-control'}} ) }}
        </div>
    </div>
    <br>
        {{ form_widget(edit_form) }}
        <input type="submit" value="Guardar" class="btn btn-block btn-success" />
    {{ form_end(edit_form) }}
{% endblock %}
