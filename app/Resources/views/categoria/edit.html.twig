{% extends 'base_logueado.html.twig' %}

{% block body %}
    <h1>Edicion de categoría</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Guardar" />
    {{ form_end(edit_form) }}

    <ul>
            <a href="{{ path('categoria_index') }}">Volver</a>
    </ul>
{% endblock %}
